[{"id": "12014d91-7e9e-4d11-8349-54080594806d", "title": "11", "content": "self.canvas = tk.Canvas(self.main_frame, bg=\"#333333\", bd=0, highlightthickness=0)\n        self.canvas.pack(expand=True, fill=\"both\", padx=15)\n\n        self.note_text = tk.Text(\n            self.canvas, wrap=\"word\", font=(\"Segoe UI\", 12), bg=\"#333333\", fg=\"white\", insertbackground=\"lightgrey\", highlightthickness=0, borderwidth=0\nd        )\n\n        self.note_text.pack(expand=True, fill=\"both\")\n\n        self.note_text.insert(\"1.0\", \"Your text content goes here\")\n\n        scrollbar = tk.Scrollbar(self.canvas, orient=\"vertical\", command=self.canvas.yview)\n        scrollbar.pack(side=\"right\", fill=\"y\")\n\n        self.canvas.create_window((0, 0), window=self.note_text, anchor=\"nw\")\n        self.canvas.config(yscrollcommand=scrollbar.set, scrollregion=self.canvas.bbox(\"all\"))\n\n        self.note_text.bind(\"<KeyRelease>\", self.auto_save)self.canvas = tk.Canvas(self.main_frame, bg=\"#333333\", bd=0, highlightthickness=0)\n        self.canvas.pack(expand=True, fill=\"both\", padx=15)\n\n        self.note_text = tk.Text(\n            self.canvas, wrap=\"word\", font=(\"Segoe UI\", 12), bg=\"#333333\", fg=\"white\", insertbackground=\"lightgrey\", highlightthickness=0, borderwidth=0\n        )\n\n        self.note_text.pack(expand=True, fill=\"both\")\n\n        self.note_text.insert(\"1.0\", \"Your text content goes here\")\n\n        scrollbar = tk.Scrollbar(self.canvas, orient=\"vertical\", command=self.canvas.yview)\n        scrollbar.pack(side=\"right\", fill=\"y\")\n\n        self.canvas.create_window((0, 0), window=self.note_text, anchor=\"nw\")\n        self.canvas.config(yscrollcommand=scrollbar.set, scrollregion=self.canvas.bbox(\"all\"))\n\n        self.note_text.bind(\"<KeyRelease>\", self.auto_save)self.canvas = tk.Canvas(self.main_frame, bg=\"#333333\", bd=0, highlightthickness=0)\n        self.canvas.pack(expand=True, fill=\"both\", padx=15)\n\n        self.note_text = tk.Text(\n            self.canvas, wrap=\"word\", font=(\"Segoe UI\", 12), bg=\"#333333\", fg=\"white\", insertbackground=\"lightgrey\", highlightthickness=0, borderwidth=0\n        )\n\n        self.note_text.pack(expand=True, fill=\"both\")\n\n        self.note_text.insert(\"1.0\", \"Your text content goes here\")\n\n        scrollbar = tk.Scrollbar(self.canvas, orient=\"vertical\", command=self.canvas.yview)\n        scrollbar.pack(side=\"right\", fill=\"y\")\n\n        self.canvas.create_window((0, 0), window=self.note_text, anchor=\"nw\")\n        self.canvas.config(yscrollcommand=scrollbar.set, scrollregion=self.canvas.bbox(\"all\"))\n\n        self.note_text.bind(\"<KeyRelease>\", self.auto_save)self.canvas = tk.Canvas(self.main_frame, bg=\"#333333\", bd=0, highlightthickness=0)\n        self.canvas.pack(expand=True, fill=\"both\", padx=15)\n\n        self.note_text = tk.Text(\n            self.canvas, wrap=\"word\", font=(\"Segoe UI\", 12), bg=\"#333333\", fg=\"white\", insertbackground=\"lightgrey\", highlightthickness=0, borderwidth=0\n        )\n\n        self.note_text.pack(expand=True, fill=\"both\")\n\n        self.note_text.insert(\"1.0\", \"Your text content goes here\")\n\n        scrollbar = tk.Scrollbar(self.canvas, orient=\"vertical\", command=self.canvas.yview)\n        scrollbar.pack(side=\"right\", fill=\"y\")\n\n        self.canvas.create_window((0, 0), window=self.note_text, anchor=\"nw\")\n        self.canvas.config(yscrollcommand=scrollbar.set, scrollregion=self.canvas.bbox(\"all\"))\n\n        self.note_text.bind(\"<KeyRelease>\", self.auto_save)self.canvas = tk.Canvas(self.main_frame, bg=\"#333333\", bd=0, highlightthickness=0)\n        self.canvas.pack(expand=True, fill=\"both\", padx=15)\n\n        self.note_text = tk.Text(\n            self.canvas, wrap=\"word\", font=(\"Segoe UI\", 12), bg=\"#333333\", fg=\"white\", insertbackground=\"lightgrey\", highlightthickness=0, borderwidth=0\n        )\n\n        self.note_text.pack(expand=True, fill=\"both\")\n\n        self.note_text.insert(\"1.0\", \"Your text content goes here\")\n\n        scrollbar = tk.Scrollbar(self.canvas, orient=\"vertical\", command=self.canvas.yview)\n        scrollbar.pack(side=\"right\", fill=\"y\")\n\n        self.canvas.create_window((0, 0), window=self.note_text, anchor=\"nw\")\n        self.canvas.config(yscrollcommand=scrollbar.set, scrollregion=self.canvas.bbox(\"all\"))\n\n        self.note_text.bind(\"<KeyRelease>\", self.auto_save)self.canvas = tk.Canvas(self.main_frame, bg=\"#333333\", bd=0, highlightthickness=0)\n        self.canvas.pack(expand=True, fill=\"both\", padx=15)\n\n        self.note_text = tk.Text(\n            self.canvas, wrap=\"word\", font=(\"Segoe UI\", 12), bg=\"#333333\", fg=\"white\", insertbackground=\"lightgrey\", highlightthickness=0, borderwidth=0\n        )\n\n        self.note_text.pack(expand=True, fill=\"both\")\n\n        self.note_text.insert(\"1.0\", \"Your text content goes here\")\n\n        scrollbar = tk.Scrollbar(self.canvas, orient=\"vertical\", command=self.canvas.yview)\n        scrollbar.pack(side=\"right\", fill=\"y\")\n\n        self.canvas.create_window((0, 0), window=self.note_text, anchor=\"nw\")\n        self.canvas.config(yscrollcommand=scrollbar.set, scrollregion=self.canvas.bbox(\"all\"))\n\n        self.note_text.bind(\"<KeyRelease>\", self.auto_save)self.canvas = tk.Canvas(self.main_frame, bg=\"#333333\", bd=0, highlightthickness=0)\n        self.canvas.pack(expand=True, fill=\"both\", padx=15)\n\n        self.note_text = tk.Text(\n            self.canvas, wrap=\"word\", font=(\"Segoe UI\", 12), bg=\"#333333\", fg=\"white\", insertbackground=\"lightgrey\", highlightthickness=0, borderwidth=0\n        )\n\n        self.note_text.pack(expand=True, fill=\"both\")\n\n        self.note_text.insert(\"1.0\", \"Your text content goes here\")\n\n        scrollbar = tk.Scrollbar(self.canvas, orient=\"vertical\", command=self.canvas.yview)\n        scrollbar.pack(side=\"right\", fill=\"y\")\n\n        self.canvas.create_window((0, 0), window=self.note_text, anchor=\"nw\")\n        self.canvas.config(yscrollcommand=scrollbar.set, scrollregion=self.canvas.bbox(\"all\"))\n\n        self.note_text.bind(\"<KeyRelease>\", self.auto_save)self.canvas = tk.Canvas(self.main_frame, bg=\"#333333\", bd=0, highlightthickness=0)\n        self.canvas.pack(expand=True, fill=\"both\", padx=15)\n\n        self.note_text = tk.Text(\n            self.canvas, wrap=\"word\", font=(\"Segoe UI\", 12), bg=\"#333333\", fg=\"white\", insertbackground=\"lightgrey\", highlightthickness=0, borderwidth=0\n        )\n\n        self.note_text.pack(expand=True, fill=\"both\")\n\n        self.note_text.insert(\"1.0\", \"Your text content goes here\")\n\n        scrollbar = tk.Scrollbar(self.canvas, orient=\"vertical\", command=self.canvas.yview)\n        scrollbar.pack(side=\"right\", fill=\"y\")\n\n        self.canvas.create_window((0, 0), window=self.note_text, anchor=\"nw\")\n        self.canvas.config(yscrollcommand=scrollbar.set, scrollregion=self.canvas.bbox(\"all\"))\n\n        self.note_text.bind(\"<KeyRelease>\", self.auto_save)self.canvas = tk.Canvas(self.main_frame, bg=\"#333333\", bd=0, highlightthickness=0)\n        self.canvas.pack(expand=True, fill=\"both\", padx=15)\n\n        self.note_text = tk.Text(\n            self.canvas, wrap=\"word\", font=(\"Segoe UI\", 12), bg=\"#333333\", fg=\"white\", insertbackground=\"lightgrey\", highlightthickness=0, borderwidth=0\n        )\n\n        self.note_text.pack(expand=True, fill=\"both\")\n\n        self.note_text.insert(\"1.0\", \"Your text content goes here\")\n\n        scrollbar = tk.Scrollbar(self.canvas, orient=\"vertical\", command=self.canvas.yview)\n        scrollbar.pack(side=\"right\", fill=\"y\")\n\n        self.canvas.create_window((0, 0), window=self.note_text, anchor=\"nw\")\n        self.canvas.config(yscrollcommand=scrollbar.set, scrollregion=self.canvas.bbox(\"all\"))\n\n        self.note_text.bind(\"<KeyRelease>\", self.auto_save)self.canvas = tk.Canvas(self.main_frame, bg=\"#333333\", bd=0, highlightthickness=0)\n        self.canvas.pack(expand=True, fill=\"both\", padx=15)\n\n        self.note_text = tk.Text(\n            self.canvas, wrap=\"word\", font=(\"Segoe UI\", 12), bg=\"#333333\", fg=\"white\", insertbackground=\"lightgrey\", highlightthickness=0, borderwidth=0\n        )\n\n        self.note_text.pack(expand=True, fill=\"both\")\n\n        self.note_text.insert(\"1.0\", \"Your text content goes here\")\n\n        scrollbar = tk.Scrollbar(self.canvas, orient=\"vertical\", command=self.canvas.yview)\n        scrollbar.pack(side=\"right\", fill=\"y\")\n\n        self.canvas.create_window((0, 0), window=self.note_text, anchor=\"nw\")\n        self.canvas.config(yscrollcommand=scrollbar.set, scrollregion=self.canvas.bbox(\"all\"))\n\n        self.note_text.bind(\"<KeyRelease>\", self.auto_save)\n", "last_modified": 1706135002.1246552}]